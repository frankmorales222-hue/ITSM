DATABASE_URL=postgresql://user:password@localhost:5432/itsm
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Temporary: used by the dev-only tickets page until auth/session is wired up.
# Set to a real users.id after seeding a test employee.
DEV_USER_ID=
